name: flutter_nfc_kit_example
description: Demonstrates how to use the flutter_nfc_kit plugin.
publish_to: 'none'

environment:
  sdk: ">=2.19.6"

dependencies:
  flutter:
    sdk: flutter
  flutter_nfc_kit:
    path: ../
  logging: ^1.2.0
  ndef: <=0.3.3
  cupertino_icons: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
